{
  "name": "timestamp-converter",
  "version": "1.0.0",
  "description": "Convert between Unix timestamps, ISO 8601 dates, and human-readable formats instantly",
  "main": "api/process.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}